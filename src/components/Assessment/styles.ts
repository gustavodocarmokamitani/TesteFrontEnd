import styled from 'styled-components';

export const Content = styled.div`
    .container {        
        font-size: 18px;
        h4 {
                font-size: 24px;
                color: var(--text);
                margin: 25px 0 3px 3px;
        }       
    }
`